### Mean

.mean() = prints the mean of the column

.median() = prints the median of the column

.max() = returns the highest value of the column

.min() = returns the smallest value of the columnn

.quantile(decimal_number) = returns the quantile of the argument

.agg(arg1, arg2, argN) = allows you to apply your own functions to a DataFrame 

.sort_values() = sort the values of the argument in ascending value by default

.cumsum() = returns the cumulative summary

.cummax() =  returns the cumulative maximum of the column